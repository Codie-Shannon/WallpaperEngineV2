<Page
    x:Class="Sketch_a_Window.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sketch_a_Window"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>
        <!-- Navigation View -->
        <!-- ======================================================= -->
        <!-- ======================================================= -->
        <NavigationView x:Name="nvNavigation" Style="{StaticResource Navigation.View}" ItemInvoked="NvNavigation_ItemInvoked">
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="nviInstalled" Icon="Save" Content="Installed"/>
            </NavigationView.MenuItems>
        </NavigationView>



        <!-- Frame -->
        <!-- ======================================================= -->
        <!-- ======================================================= -->
        <Frame x:Name="fFrame" Style="{StaticResource MainPage.Frame}"/>
    </Grid>
</Page>